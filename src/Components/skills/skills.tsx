import React from 'react';
import { Link } from 'react-router-dom';
import {
  projectsRoute,
  aboutRoute,
  skillsRoute,
  kataRoute,
  contactRoutes,
} from '../../Config/Navigation/routes';
import './TopNav.scss';

const TopNav: React.FC = () => {
  return (
  <div>Here will be images with links to relevant demonstrations of highlighted skills</div>
  );
};

export default TopNav;
